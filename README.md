Sources for the plugin `shared.google.play.services.analytics`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.analytics" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
